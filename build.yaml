include:
  - board: nice_nano_v2
    shield: corne_left
    snippet: studio-rpc-usb-uart
    cmake-args: -DCONFIG_ZMK_STUDIO=y
  - board: nice_nano_v2
    shield: corne_right
  # Troubleshooting: uncomment the next line, rebuild, and flash
  # the settings_reset firmware to either half to clear stuck
  # Bluetooth pairings. Re-comment afterwards.
  # - board: nice_nano_v2
  #   shield: settings_reset
